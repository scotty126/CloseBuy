[build]
command = "(corepack enable || true) && pnpm install --frozen-lockfile && npx turbo run build --filter=@closebuy/vendor"
publish = "apps/vendor/.next"

[[plugins]]
package = "@netlify/plugin-nextjs"
